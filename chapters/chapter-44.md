# Часть XI. Unsafe Rust

## Глава 44. Что действительно означает `unsafe`

До этой главы мы писали только безопасный код. Rust гарантировал, что мы не сделаем ошибок с памятью, не вызовем неопределённое поведение и не создадим гонок данных. Но иногда безопасных абстракций недостаточно — нужно работать с памятью напрямую, взаимодействовать с C-кодом или реализовывать эффективные структуры данных.

Для таких случаев в Rust есть `unsafe`. Но `unsafe` — это не отключение проверок компилятора. Это **обещание программиста**: "Я знаю, что делаю, и гарантирую безопасность".

В этой главе мы разберёмся, что такое `unsafe`, какие операции он разрешает, и как использовать его ответственно.

Все примеры этой главы используют **Rust Edition 2024**.

---

## 44.1. Что такое `unsafe`?

`unsafe` — это механизм Rust, который позволяет выполнять несколько операций, безопасность которых компилятор не может доказать автоматически.

Важно понимать принципиальную вещь:

> **`unsafe` не означает «этот код небезопасен» и не означает «этот код безопасен».**
>
> `unsafe` означает: **«компилятор не может доказать безопасность этой операции; программист должен обеспечить необходимые условия»**.

Например, компилятор не может самостоятельно определить, является ли произвольный сырой указатель действительным:

```rust
fn main() {
    let x = 42;

    let ptr = &x as *const i32;

    unsafe {
        println!("{ptr:?}");
        println!("{}", *ptr);
    }
}
```

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=fn+main%28%29++%7B%0A++++let+x+%3D+42%3B%0A++++let+ptr+%3D+%26x+as+%2Aconst+i32%3B%0A%0A++++unsafe+%7B%0A++++++++println%21%28%22%7Bptr%3A%3F%7D%22%2C+ptr%29%3B%0A++++++++println%21%28%22%7B%7D%22%2C+%2Aptr%29%3B%0A++++%7D%0A%7D)

Здесь создание сырого указателя безопасно, а его разыменование требует `unsafe`.

При этом компилятор продолжает выполнять огромное количество проверок:

- проверку типов;
- проверку заимствований в безопасной части программы;
- проверку времени жизни;
- проверку синтаксиса;
- проверку большинства правил языка;
- проверку границ массивов при обычной индексации;
- проверку многих других условий.

`unsafe` **не превращает Rust в C/C++**.

### `unsafe` как граница доверия

Хороший способ мыслить об `unsafe`:

```text
Safe Rust
    │
    │ компилятор может доказать безопасность
    ▼
unsafe boundary
    │
    │ программист обязан доказать дополнительные условия
    ▼
низкоуровневая операция
```

Например:

```rust
unsafe {
    *ptr
}
```

Сам `unsafe`-блок ничего не доказывает. Он лишь говорит компилятору:

> «Я осознаю, что эта операция требует дополнительных гарантий».

Если `ptr` недействителен, программа всё равно может содержать **undefined behavior (UB)**.

---

## 44.2. Возможности `unsafe`

У `unsafe` есть несколько конкретных возможностей. В учебном контексте их удобно разделить на пять основных категорий:

| Операция                       | Что позволяет                                                       |
| ------------------------------ | ------------------------------------------------------------------- |
| Разыменование сырого указателя | Читать или записывать через `*const T` / `*mut T`                   |
| Вызов `unsafe fn`              | Вызывать функции с явно заданным safety contract                    |
| Доступ к `static mut`          | Работать с изменяемой глобальной памятью                            |
| Реализация `unsafe trait`      | Вручную подтвердить выполнение контракта трейта                     |
| Доступ к полям `union`         | Читать поле `union`, когда программист знает, что оно действительно |

Кроме того, современный Rust имеет отдельные правила для некоторых других unsafe-конструкций, например `unsafe extern` блоков и `unsafe`-атрибутов. Но эти конструкции лучше рассматривать отдельно, поскольку они относятся к разным аспектам безопасности.

### 1. Разыменование сырого указателя

```rust
let mut value = 42;
let ptr = &mut value as *mut i32;

unsafe {
    *ptr = 100;
}

println!("{value}");
```

### 2. Вызов `unsafe fn`

```rust
unsafe fn dangerous() {
    // ...
}

unsafe {
    dangerous();
}
```

### 3. Работа с `static mut`

`static mut` — особенно опасный механизм. В современном Rust его использование требует повышенного внимания к конкурирующему доступу и правилам aliasing.

```rust
static mut COUNTER: u32 = 0;

unsafe {
    // Доступ к mutable static требует unsafe.
    COUNTER += 1;
}
```

Для нового кода обычно следует предпочитать безопасные альтернативы, например атомарные типы:

```rust
use std::sync::atomic::{AtomicU32, Ordering};

static COUNTER: AtomicU32 = AtomicU32::new(0);

fn main() {
    COUNTER.fetch_add(1, Ordering::Relaxed);

    println!("{}", COUNTER.load(Ordering::Relaxed));
}
```

Таким образом, сам факт существования `static mut` ещё не означает, что его следует использовать.

### 4. Реализация `unsafe trait`

```rust
unsafe trait MyUnsafeTrait {
    // Safety contract определяется автором трейта.
}

unsafe impl MyUnsafeTrait for i32 {}
```

Здесь компилятор доверяет программисту: он не доказывает, что `i32` действительно удовлетворяет safety contract трейта.

### 5. Чтение поля `union`

```rust
union Value {
    integer: u32,
    float: f32,
}

fn main() {
    let value = Value { integer: 42 };

    unsafe {
        println!("{}", value.integer);
    }
}
```

`union` не хранит информацию о том, какое поле в данный момент является логически активным. Поэтому чтение поля требует `unsafe`.

---

## 44.3. Сырые указатели (`*const T`, `*mut T`)

Сырые указатели — один из главных инструментов низкоуровневого Rust.

```rust
*const T
*mut T
```

Они существенно отличаются от обычных ссылок `&T` и `&mut T`.

Сырой указатель:

- может быть `null`;
- не обязан указывать на объект, который сейчас существует;
- не имеет обычных гарантий Rust-ссылки;
- может иметь неправильное выравнивание;
- не гарантирует уникальность изменяемого доступа;
- не гарантирует валидность памяти;
- может быть получен из операций низкого уровня;
- может использоваться для FFI.

Создание сырого указателя само по себе обычно безопасно:

```rust
let value = 42;
let ptr = &value as *const i32;
```

Но разыменование требует `unsafe`:

```rust
unsafe {
    println!("{}", *ptr);
}
```

### Сырой указатель может быть `null`

```rust
fn main() {
    let ptr: *const i32 = std::ptr::null();

    println!("is null: {}", ptr.is_null());

    // Это UB и делать так нельзя:
    //
    // unsafe {
    //     println!("{}", *ptr);
    // }
}
```

Важно:

> Разыменование `null` — **не «обычная ошибка» и не гарантированная паника**. Это undefined behavior.

---

### `Box::into_raw` и `Box::from_raw`

Сырые указатели часто используются при ручном управлении владением:

```rust
fn main() {
    let value = Box::new(42);

    let ptr = Box::into_raw(value);

    unsafe {
        println!("{}", *ptr);

        // Возвращаем владение Box.
        drop(Box::from_raw(ptr));
    }
}
```

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=fn+main%28%29++%7B%0A++++let+value+%3D+Box%3A%3Anew%2842%29%3B%0A++++let+ptr+%3D+Box%3A%3Ainto_raw%28value%29%3B%0A%0A++++unsafe+%7B%0A++++++++println%21%28%22%7B%7D%22%2C+%2Aptr%29%3B%0A++++++++drop%28Box%3A%3Afrom_raw%28ptr%29%29%3B%0A++++%7D%0A%7D)

Здесь есть важный контракт:

> Указатель, переданный в `Box::from_raw`, должен быть указателем, полученным таким способом, который соответствует требованиям `Box`, и владение этой аллокацией не должно уже находиться у другого владельца.

Нельзя бездумно делать:

```rust
unsafe {
    let a = Box::from_raw(ptr);
    let b = Box::from_raw(ptr);
}
```

Это может привести к двойному освобождению памяти.

---

## 44.4. `unsafe fn`

`unsafe fn` сообщает вызывающему коду:

> «Эта функция имеет дополнительные требования безопасности, которые компилятор не может проверить».

Например:

```rust
/// Reads an `i32` from `ptr`.
///
/// # Safety
///
/// `ptr` must be non-null, properly aligned, point to a valid
/// initialized `i32`, and remain valid for the duration of the read.
unsafe fn read_i32(ptr: *const i32) -> i32 {
    unsafe {
        *ptr
    }
}

fn main() {
    let value = 42;
    let ptr = &value as *const i32;

    let result = unsafe {
        read_i32(ptr)
    };

    println!("{result}");
}
```

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=%2F%2F%2F+Reads+an+%60i32%60+from+%60ptr%60.%0A%2F%2F%2F%0A%2F%2F%2F+%23+Safety%0A%2F%2F%2F%0A%2F%2F%2F+%60ptr%60+must+be+non-null%2C+properly+aligned%2C+point+to+a+valid%0A%2F%2F%2F+initialized+%60i32%60%2C+and+remain+valid+for+the+duration+of+the+read.%0Aunsafe+fn+read_i32%28ptr%3A+%2Aconst+i32%29+-%3E+i32+%7B%0A++++unsafe++%7B%0A++++++++%2Aptr%0A++++%7D%0A%7D%0A%0Afn+main%28%29+%7B%0A++++let+value+%3D+42%3B%0A++++let+ptr+%3D+%26value+as+%2Aconst+i32%3B%0A%0A++++let+result+%3D+unsafe+%7B%0A++++++++read_i32%28ptr%29%0A++++%7D%3B%0A%0A++++println%21%28%22%7Bresult%7D%22%29%3B%0A%7D)

### `unsafe fn` не делает всё её тело автоматически `unsafe`

Это особенно важно в современном Rust.

Если внутри `unsafe fn` выполняется unsafe-операция, её рекомендуется явно помещать в `unsafe`-блок:

```rust
unsafe fn read_i32(ptr: *const i32) -> i32 {
    unsafe {
        *ptr
    }
}
```

Так граница каждого небезопасного действия остаётся видимой.

Это особенно полезно при включённом lint:

```rust
#![warn(unsafe_op_in_unsafe_fn)]
```

Такой стиль позволяет отличать:

```rust
unsafe fn
```

от:

```rust
unsafe {
    // конкретная unsafe-операция
}
```

Первая конструкция является частью **API и safety contract**, вторая — конкретным местом, где выполняется операция, требующая доверия.

---

## 44.5. Safety Contracts и Invariants

Чтобы писать `unsafe` правильно, необходимо понимать два понятия:

- **safety contract** — условия, которые должны быть выполнены для безопасного использования операции;
- **invariant** — свойство, которое должно оставаться истинным на протяжении существования структуры или абстракции.

Например, рассмотрим структуру, хранящую указатель и длину:

```rust
use std::marker::PhantomData;

struct MySlice<'a> {
    ptr: *const i32,
    len: usize,
    marker: PhantomData<&'a [i32]>,
}
```

У такой структуры есть инварианты:

1. `ptr` должен указывать на начало массива элементов `i32`;
2. первые `len` элементов должны существовать;
3. память должна быть корректно выровнена;
4. элементы должны быть инициализированы;
5. память должна оставаться живой всё время жизни `'a`.

Создание такой структуры из произвольного указателя поэтому является `unsafe`:

```rust
impl<'a> MySlice<'a> {
    /// Creates a `MySlice` from a raw pointer and length.
    ///
    /// # Safety
    ///
    /// - `ptr` must be valid for reads of `len` consecutive `i32` values.
    /// - `ptr` must be properly aligned.
    /// - The memory must remain valid for lifetime `'a`.
    /// - The elements must be initialized.
    pub unsafe fn from_raw_parts(ptr: *const i32, len: usize) -> Self {
        Self {
            ptr,
            len,
            marker: PhantomData,
        }
    }

    pub fn len(&self) -> usize {
        self.len
    }

    pub fn get(&self, index: usize) -> Option<i32> {
        if index >= self.len {
            return None;
        }

        unsafe {
            Some(*self.ptr.add(index))
        }
    }
}
```

Использование:

```rust
fn main() {
    let values = [10, 20, 30, 40];

    let slice = unsafe {
        MySlice::from_raw_parts(values.as_ptr(), values.len())
    };

    println!("length = {}", slice.len());
    println!("item 2 = {:?}", slice.get(2));
}
```

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=use+std%3A%3Amarker%3A%3APhantomData%3B%0A%0Astruct+MySlice%3C%27a%3E+%7B%0A++++ptr%3A+%2Aconst+i32%2C%0A++++len%3A+usize%2C%0A++++marker%3A+PhantomData%3C%26%27a+%5Bi32%5D%3E%2C%0A%7D%0A%0Aimpl%3C%27a%3E+MySlice%3C%27a%3E+%7B%0A++++%2F%2F%2F+%23+Safety%0A++++%2F%2F%2F%0A++++%2F%2F%2F+-+%60ptr%60+must+be+valid+for+reads+of+%60len%60+consecutive+%60i32%60+values.%0A++++%2F%2F%2F+-+%60ptr%60+must+be+properly+aligned.%0A++++%2F%2F%2F+-+The+memory+must+remain+valid+for+lifetime+%60%27a%60.%0A++++%2F%2F%2F+-+The+elements+must+be+initialized.%0A++++pub+unsafe+fn+from_raw_parts%28ptr%3A+%2Aconst+i32%2C+len%3A+usize%29+-%3E+Self+%7B%0A++++++++Self++%7B+ptr%2C+len%2C+marker%3A+PhantomData+%7D%0A++++%7D%0A%0A++++pub+fn+len%28%26self%29+-%3E+usize+%7B%0A++++++++self.len%0A++++%7D%0A%0A++++pub+fn+get%28%26self%2C+index%3A+usize%29+-%3E+Option%3Ci32%3E+%7B%0A++++++++if+index+%3E%3D+self.len++%7B+%0A++++++++++++return+None%3B%0A++++++++%7D%0A%0A++++++++unsafe+%7B%0A++++++++++++Some%28%2Aself.ptr.add%28index%29%29%0A++++++++%7D%0A++++%7D%0A%7D%0A%0Afn+main%28%29+%7B%0A++++let+values+%3D+%5B10%2C+20%2C+30%2C+40%5D%3B%0A%0A++++let+slice+%3D+unsafe+%7B%0A++++++++MySlice%3A%3Afrom_raw_parts%28values.as_ptr%28%29%2C+values.len%28%29%29%0A++++%7D%3B%0A%0A++++println%21%28%22length+%3D+%7B%7D%22%2C+slice.len%28%29%29%3B%0A++++println%21%28%22item+2+%3D+%7B%3A%3F%7D%22%2C+slice.get%282%29%29%3B%0A%7D)

Здесь `PhantomData<&'a [i32]>` важен не для выполнения чтения, а для **моделирования отношения владения/заимствования в типовой системе**.

Именно такие инварианты позволяют превратить локальный `unsafe` в безопасный API.

---

## 44.6. Безопасные абстракции над `unsafe`

Главная идея `unsafe` в хорошо спроектированном Rust-коде выглядит так:

```text
unsafe implementation
        │
        ▼
проверяем invariants
        │
        ▼
безопасная абстракция
        │
        ▼
safe API
```

Пользователь безопасного API не должен каждый раз самостоятельно доказывать те же условия.

### Пример: собственный владеющий указатель

Если нам действительно нужен низкоуровневый указатель, можно инкапсулировать его:

```rust
use std::{
    marker::PhantomData,
    ptr::NonNull,
};

pub struct SafePtr<T> {
    ptr: NonNull<T>,
    marker: PhantomData<T>,
}

impl<T> SafePtr<T> {
    pub fn new(value: T) -> Self {
        let ptr = Box::into_raw(Box::new(value));

        // Box::into_raw возвращает ненулевой указатель.
        let ptr = unsafe {
            NonNull::new_unchecked(ptr)
        };

        Self {
            ptr,
            marker: PhantomData,
        }
    }

    pub fn get(&self) -> &T {
        unsafe {
            self.ptr.as_ref()
        }
    }

    pub fn get_mut(&mut self) -> &mut T {
        unsafe {
            self.ptr.as_mut()
        }
    }
}

impl<T> Drop for SafePtr<T> {
    fn drop(&mut self) {
        unsafe {
            drop(Box::from_raw(self.ptr.as_ptr()));
        }
    }
}

fn main() {
    let mut value = SafePtr::new(42);

    println!("{}", value.get());

    *value.get_mut() = 100;

    println!("{}", value.get());
}
```

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=use+std%3A%3A%7Bmarker%3A%3APhantomData%2C+ptr%3A%3ANonNull%7D%3B%0A%0Apub+struct+SafePtr%3CT%3E+%7B%0A++++ptr%3A+NonNull%3CT%3E%2C%0A++++marker%3A+PhantomData%3CT%3E%2C%0A%7D%0A%0Aimpl%3CT%3E+SafePtr%3CT%3E+%7B%0A++++pub+fn+new%28value%3A+T%29+-%3E+Self+%7B%0A++++++++let+ptr+%3D+Box%3A%3Ainto_raw%28Box%3A%3Anew%28value%29%29%3B%0A++++++++let+ptr+%3D+unsafe+%7B+NonNull%3A%3Anew_unchecked%28ptr%29+%7D%3B%0A%0A++++++++Self+%7B+ptr%2C+marker%3A+PhantomData+%7D%0A++++%7D%0A%0A++++pub+fn+get%28%26self%29+-%3E+%26T+%7B%0A++++++++unsafe+%7B+self.ptr.as_ref%28%29+%7D%0A++++%7D%0A%0A++++pub+fn+get_mut%28%26mut+self%29+-%3E+%26mut+T+%7B%0A++++++++unsafe+%7B+self.ptr.as_mut%28%29+%7D%0A++++%7D%0A%7D%0A%0Aimpl%3CT%3E+Drop+for+SafePtr%3CT%3E+%7B%0A++++fn+drop%28%26mut+self%29+%7B%0A++++++++unsafe+%7B+drop%28Box%3A%3Afrom_raw%28self.ptr.as_ptr%28%29%29%29+%7D%0A++++%7D%0A%7D%0A%0Afn+main%28%29+%7B%0A++++let+mut+value+%3D+SafePtr%3A%3Anew%2842%29%3B%0A++++println%21%28%22%7B%7D%22%2C+value.get%28%29%29%3B%0A++++%2Avalue.get_mut%28%29+%3D+100%3B%0A++++println%21%28%22%7B%7D%22%2C+value.get%28%29%29%3B%0A%7D)

Обратите внимание: снаружи нет ни одного `unsafe`.

Это и есть один из главных признаков хорошей unsafe-абстракции:

```rust
let mut value = SafePtr::new(42);

*value.get_mut() = 100;
```

Пользователь работает с обычным безопасным API, а все опасные операции сосредоточены внутри реализации.

При этом наличие `unsafe` внутри реализации **само по себе не делает абстракцию безопасной**. Автор должен доказать, что инварианты действительно сохраняются при любых допустимых вызовах публичного API.

---

## 44.7. `unsafe` Traits

`unsafe trait` используется тогда, когда сам факт реализации трейта должен гарантировать некоторое свойство, которое компилятор не способен проверить автоматически.

```rust
/// Marker trait for types satisfying a custom safety contract.
///
/// # Safety
///
/// Implementors must satisfy the invariants required by the
/// code that relies on this trait.
unsafe trait Sendable {}

unsafe impl Sendable for i32 {}
```

Почему `unsafe impl`?

Потому что компилятор не может проверить произвольный safety contract трейта.

### Важное отличие от обычного трейта

Для обычного трейта:

```rust
trait MyTrait {
    fn method(&self);
}
```

компилятор проверяет, что реализация соответствует требованиям самого языка: методы существуют, типы совпадают и т. д.

Для `unsafe trait` существует ещё один уровень:

```text
компилятор проверяет синтаксис и типы
                    +
программист гарантирует safety contract
```

Поэтому `unsafe impl` следует рассматривать как **утверждение программиста**, а не как способ получить дополнительные возможности.

---

## 44.9. Как документировать safety requirements

Для каждой публичной `unsafe fn` необходимо явно документировать её safety contract.

Стандартный формат:

```rust
/// Reads a value from a raw pointer.
///
/// # Safety
///
/// - `ptr` must be non-null.
/// - `ptr` must be properly aligned for `T`.
/// - `ptr` must point to a valid initialized `T`.
/// - The pointed-to memory must be valid for the duration of the read.
pub unsafe fn read_from_ptr<T>(ptr: *const T) -> T {
    unsafe {
        ptr.read()
    }
}
```

Здесь важно не просто написать:

```rust
/// # Safety
/// Caller must ensure the pointer is valid.
```

Это слишком расплывчато.

Хороший safety contract должен отвечать на вопрос:

> **Что именно вызывающий обязан гарантировать?**

Например:

```text
ptr != null
ptr корректно выровнен
память существует
значение инициализировано
доступ разрешён
память не освобождена
требования aliasing соблюдены
```

Конкретные требования зависят от операции.

### `# Safety` — часть API

Документация `unsafe fn` фактически является частью контракта API:

```rust
unsafe fn operation(...)
```

означает:

> «Перед вызовом прочитайте `# Safety` и докажите, что его условия выполнены».

Поэтому safety-документация должна быть настолько конкретной, чтобы другой разработчик мог проверить её выполнение.

---

## 🔨 Эксперименты с компилятором

### Эксперимент 1: Разыменование `null`

```rust
fn main() {
    let ptr: *const i32 = std::ptr::null();

    unsafe {
        println!("{}", *ptr);
    }
}
```

Этот код может успешно пройти компиляцию, потому что `unsafe` разрешает саму операцию.

Но это **не делает операцию корректной**.

Разыменование `null` приводит к undefined behavior.

Не следует ожидать обязательного результата вроде:

```text
panic
```

или:

```text
segmentation fault
```

При UB компилятор не обязан предоставлять какой-либо конкретный результат.

---

### Эксперимент 2: Неправильное выравнивание

Следующий пример намеренно создаёт потенциально неправильно выровненный указатель:

```rust
fn main() {
    let bytes = [0u8; 8];

    let ptr = unsafe {
        bytes.as_ptr().add(1) as *const u32
    };

    unsafe {
        println!("{}", *ptr);
    }
}
```

Проблема здесь не только в том, что адрес может быть неправильно выровнен для `u32`.

Чтобы безопасно разыменовать `ptr`, необходимо также гарантировать:

- корректное выравнивание;
- достаточный размер доступной памяти;
- валидность указателя;
- инициализированность `u32`;
- соблюдение остальных требований к доступу через указатель.

Поэтому такой код нельзя использовать как способ «прочитать четыре байта».

Для работы с байтами следует использовать соответствующие безопасные операции, например `u32::from_ne_bytes`, `u32::from_le_bytes` или `u32::from_be_bytes`.

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=fn+main%28%29+%7B%0A++++let+bytes+%3D+%5B0u8%3B+8%5D%3B%0A%0A++++let+ptr+%3D+unsafe+%7B%0A++++++++bytes.as_ptr%28%29.add%281%29+as+%2Aconst+u32%0A++++%7D%3B%0A%0A++++unsafe+%7B%0A++++++++println%21%28%22%7B%7D%22%2C+%2Aptr%29%3B%0A++++%7D%0A%7D)

---

## Практика

### Задание 1. Сырой указатель

Создайте переменную:

```rust
let mut value = 42;
```

Получите `*mut i32`, измените значение через сырой указатель и выведите результат.

Обязательно объясните, **почему** разыменование указателя требует `unsafe`.

---

### Задание 2. `unsafe fn` и safety contract

Напишите:

```rust
unsafe fn read_i32(ptr: *const i32) -> i32
```

и документируйте её через `# Safety`.

Ваш контракт должен явно описывать:

- требования к `null`;
- выравнивание;
- валидность памяти;
- инициализированность значения.

Затем вызовите функцию с корректным указателем.

---

### Задание 3. Безопасная абстракция

Создайте структуру, которая внутри хранит сырой указатель, но предоставляет безопасные методы:

```rust
fn get(&self) -> &T
fn get_mut(&mut self) -> &mut T
```

Опишите invariants, которые должны сохраняться.

Цель задания — не просто заставить код работать, а доказать:

> почему пользователь безопасного API не может нарушить safety contract.

---

### Задание 4. Найдите UB

Исследуйте следующий код:

```rust
let ptr: *const i32 = std::ptr::null();

unsafe {
    println!("{}", *ptr);
}
```

Ответьте:

1. Почему компилятор разрешает этот код?
2. Почему это не означает, что код корректен?
3. Почему результат нельзя описать как «Rust гарантированно вызовет panic»?

---

### Задание 5. `unsafe` без необходимости

Сравните:

```rust
unsafe {
    println!("{}", 42);
}
```

с:

```rust
println!("{}", 42);
```

Объясните, почему первый вариант не даёт никаких преимуществ.

Затем найдите в своей реализации пример, где `unsafe` можно локализовать в одном небольшом блоке вместо большого участка функции.

---

### Задание 6. Safety review

Для следующей функции напишите полный safety contract:

```rust
unsafe fn copy_value<T>(src: *const T, dst: *mut T) {
    unsafe {
        std::ptr::copy_nonoverlapping(src, dst, 1);
    }
}
```

Подумайте, какие условия должны выполняться для:

- `src`;
- `dst`;
- размера `T`;
- инициализированности;
- перекрытия областей памяти;
- допустимости записи в `dst`.

Это уже не упражнение на синтаксис `unsafe`, а упражнение на **анализ safety contract**.

---

## Главное из этой главы

После этой главы мы понимаем:

- **`unsafe`** — не отключение проверок компилятора, а механизм явного доверия программисту.
- **Raw pointers** — `*const T` и `*mut T`, которые не обладают гарантиями обычных ссылок.
- **`unsafe fn`** — функция с дополнительными требованиями безопасности к вызывающему.
- **`unsafe {}`** — локальная граница конкретной unsafe-операции.
- **`unsafe trait`** — трейт с safety contract, который нельзя полностью проверить компилятором.
- **`union`** — низкоуровневая конструкция, чтение полей которой требует `unsafe`.
- **Safety contract** — явное описание условий, необходимых для безопасного выполнения операции.
- **Invariants** — свойства, которые должны сохраняться внутри безопасной абстракции.
- **`PhantomData`** — инструмент для выражения в типовой системе отношений владения, заимствования и времени жизни, которые не представлены непосредственно хранимыми данными.
- **Безопасная абстракция** — способ локализовать `unsafe` внутри реализации и предоставить безопасный API.
- **Undefined behavior** — ситуация, при которой программа нарушает требования модели Rust; `unsafe` не делает UB допустимым.

**Самая важная идея:**

> `unsafe` не означает: «теперь компилятор мне не мешает».
>
> `unsafe` означает: **«здесь существует свойство безопасности, которое компилятор не способен доказать автоматически, и я беру на себя ответственность за его выполнение»**.
>
> Хороший `unsafe`-код поэтому начинается не с вопроса «как заставить это скомпилироваться?», а с вопросов:
>
> 1. Какой safety contract существует?
> 2. Какие invariants необходимы?
> 3. Где именно находится unsafe-операция?
> 4. Можно ли доказать её корректность?
> 5. Можно ли спрятать её за безопасным API?
>
> Чем меньше область `unsafe`, чем точнее safety contract и чем лучше изолированы invariants, тем надёжнее низкоуровневый Rust-код.

---

## 44.10. Когда использовать `unsafe`

**Хорошие причины использовать `unsafe`:**

1. **Взаимодействие с FFI (C-кодом).**
2. **Реализация эффективных структур данных.**
3. **Оптимизация горячих путей.**
4. **Работа с памятью на низком уровне.**

**Плохие причины использовать `unsafe`:**

1. "Я знаю, что делаю" (без реального понимания).
2. "Так быстрее" (без доказательств).
3. "Мне лень переделывать" (без необходимости).

---

## 🔨 Эксперименты с компилятором

### Эксперимент 1: Разыменование `null`

```rust
fn main() {
    let ptr: *const i32 = std::ptr::null();

    unsafe {
        println!("{}", *ptr);
    }
}
```

Этот код может успешно пройти компиляцию, потому что `unsafe` разрешает саму операцию.

Но это **не делает операцию корректной**.

Разыменование `null` приводит к undefined behavior.

Не следует ожидать обязательного результата вроде:

```text
panic
```

или:

```text
segmentation fault
```

При UB компилятор не обязан предоставлять какой-либо конкретный результат.

---

### Эксперимент 2: Неправильное выравнивание

Следующий пример намеренно создаёт потенциально неправильно выровненный указатель:

```rust
fn main() {
    let bytes = [0u8; 8];

    let ptr = unsafe {
        bytes.as_ptr().add(1) as *const u32
    };

    unsafe {
        println!("{}", *ptr);
    }
}
```

Проблема здесь не только в том, что адрес может быть неправильно выровнен для `u32`.

Чтобы безопасно разыменовать `ptr`, необходимо также гарантировать:

- корректное выравнивание;
- достаточный размер доступной памяти;
- валидность указателя;
- инициализированность `u32`;
- соблюдение остальных требований к доступу через указатель.

Поэтому такой код нельзя использовать как способ «прочитать четыре байта».

Для работы с байтами следует использовать соответствующие безопасные операции, например `u32::from_ne_bytes`, `u32::from_le_bytes` или `u32::from_be_bytes`.

[Открыть пример в Rust Playground](https://play.rust-lang.org/?version=stable&mode=debug&edition=2024&code=fn+main%28%29+%7B%0A++++let+bytes+%3D+%5B0u8%3B+8%5D%3B%0A%0A++++let+ptr+%3D+unsafe+%7B%0A++++++++bytes.as_ptr%28%29.add%281%29+as+%2Aconst+u32%0A++++%7D%3B%0A%0A++++unsafe+%7B%0A++++++++println%21%28%22%7B%7D%22%2C+%2Aptr%29%3B%0A++++%7D%0A%7D)

---

## Практика

### Задание 1. Сырой указатель

Создайте переменную:

```rust
let mut value = 42;
```

Получите `*mut i32`, измените значение через сырой указатель и выведите результат.

Обязательно объясните, **почему** разыменование указателя требует `unsafe`.

---

### Задание 2. `unsafe fn` и safety contract

Напишите:

```rust
unsafe fn read_i32(ptr: *const i32) -> i32
```

и документируйте её через `# Safety`.

Ваш контракт должен явно описывать:

- требования к `null`;
- выравнивание;
- валидность памяти;
- инициализированность значения.

Затем вызовите функцию с корректным указателем.

---

### Задание 3. Безопасная абстракция

Создайте структуру, которая внутри хранит сырой указатель, но предоставляет безопасные методы:

```rust
fn get(&self) -> &T
fn get_mut(&mut self) -> &mut T
```

Опишите invariants, которые должны сохраняться.

Цель задания — не просто заставить код работать, а доказать:

> почему пользователь безопасного API не может нарушить safety contract.

---

### Задание 4. Найдите UB

Исследуйте следующий код:

```rust
let ptr: *const i32 = std::ptr::null();

unsafe {
    println!("{}", *ptr);
}
```

Ответьте:

1. Почему компилятор разрешает этот код?
2. Почему это не означает, что код корректен?
3. Почему результат нельзя описать как «Rust гарантированно вызовет panic»?

---

### Задание 5. `unsafe` без необходимости

Сравните:

```rust
unsafe {
    println!("{}", 42);
}
```

с:

```rust
println!("{}", 42);
```

Объясните, почему первый вариант не даёт никаких преимуществ.

Затем найдите в своей реализации пример, где `unsafe` можно локализовать в одном небольшом блоке вместо большого участка функции.

---

### Задание 6. Safety review

Для следующей функции напишите полный safety contract:

```rust
unsafe fn copy_value<T>(src: *const T, dst: *mut T) {
    unsafe {
        std::ptr::copy_nonoverlapping(src, dst, 1);
    }
}
```

Подумайте, какие условия должны выполняться для:

- `src`;
- `dst`;
- размера `T`;
- инициализированности;
- перекрытия областей памяти;
- допустимости записи в `dst`.

Это уже не упражнение на синтаксис `unsafe`, а упражнение на **анализ safety contract**.

---

## Главное из этой главы

После этой главы мы понимаем:

- **`unsafe`** — не отключение проверок компилятора, а механизм явного доверия программисту.
- **Raw pointers** — `*const T` и `*mut T`, которые не обладают гарантиями обычных ссылок.
- **`unsafe fn`** — функция с дополнительными требованиями безопасности к вызывающему.
- **`unsafe {}`** — локальная граница конкретной unsafe-операции.
- **`unsafe trait`** — трейт с safety contract, который нельзя полностью проверить компилятором.
- **`union`** — низкоуровневая конструкция, чтение полей которой требует `unsafe`.
- **Safety contract** — явное описание условий, необходимых для безопасного выполнения операции.
- **Invariants** — свойства, которые должны сохраняться внутри безопасной абстракции.
- **`PhantomData`** — инструмент для выражения в типовой системе отношений владения, заимствования и времени жизни, которые не представлены непосредственно хранимыми данными.
- **Безопасная абстракция** — способ локализовать `unsafe` внутри реализации и предоставить безопасный API.
- **Undefined behavior** — ситуация, при которой программа нарушает требования модели Rust; `unsafe` не делает UB допустимым.

**Самая важная идея:**

> `unsafe` не означает: «теперь компилятор мне не мешает».
>
> `unsafe` означает: **«здесь существует свойство безопасности, которое компилятор не способен доказать автоматически, и я беру на себя ответственность за его выполнение»**.
>
> Хороший `unsafe`-код поэтому начинается не с вопроса «как заставить это скомпилироваться?», а с вопросов:
>
> 1. Какой safety contract существует?
> 2. Какие invariants необходимы?
> 3. Где именно находится unsafe-операция?
> 4. Можно ли доказать её корректность?
> 5. Можно ли спрятать её за безопасным API?
>
> Чем меньше область `unsafe`, чем точнее safety contract и чем лучше изолированы invariants, тем надёжнее низкоуровневый Rust-код.
